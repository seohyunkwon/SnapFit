<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.snapfit.model.dao.LikeCommentDao">
	<!-- 좋아요 생성 -->
	<insert id="insertLikeVideo" parameterType="LikeComment">
		INSERT INTO like_comment (comment_no, member_no) 
		VALUES
		(#{commentNo}, #{memberNo})
	</insert>
	
	<!-- 좋아요 삭제 -->
	<delete id="deleteLikeVideo" parameterType="LikeVideo">
		DELETE FROM like_video 
		WHERE 
		video_no = #{videoNo}
		AND
		member_no = #{memberNo}
	</delete>
	
	<!-- 좋아요 여부 확인 -->
	<select id="selectLikeVideo" >
		SELECT count(*) FROM like_video 
		WHERE
		video_no = #{videoNo}
		AND
		member_no = #{memberNo} 
	</select>
</mapper>